{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":"<p>Uses simple, composable logic to validate data.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install git+https://github.com/Dog-Egg/Zangar\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<pre><code>&gt;&gt;&gt; import zangar as z\n</code></pre> <p>Zangar's core validation logic for data consists of only two methods, defined as <code>ensure</code> and <code>transform</code>, which can be combined through chain-like calls.</p> <p>For example:</p> <pre><code>&gt;&gt;&gt; name_to_greeting = (\n...     z.ensure(lambda s: isinstance(s, str))\n...     .transform(lambda s: s.title())\n...     .transform(lambda s: f\"Hello {s}\")\n...     .ensure(lambda s: \"!\" not in s)\n... )\n\n&gt;&gt;&gt; name_to_greeting.parse('john')\n'Hello John'\n\n&gt;&gt;&gt; name_to_greeting.parse('john!')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Invalid value']}]\n</code></pre> <p>All other validation methods provided by Zangar are implemented by combining <code>ensure</code> and <code>transform</code>.</p>"},{"location":"#schema","title":"Schema","text":""},{"location":"#ensure","title":"<code>.ensure</code>","text":"<p><code>ensure</code> is used to define custom data validation rules and should return a boolean value.</p> <pre><code>&gt;&gt;&gt; my_data = z.ensure(lambda s: len(s) &gt; 5)\n\n&gt;&gt;&gt; my_data.parse('hello')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Invalid value']}]\n\n&gt;&gt;&gt; my_data.parse('hello world') # this is OK.\n'hello world'\n</code></pre>"},{"location":"#message","title":"message","text":"<p>A <code>message</code> can be provided for a validation method, which will be used when validation fails. If the <code>message</code> is a callable object, its return value will be used as the <code>message</code>.</p> <pre><code>&gt;&gt;&gt; my_data = z.ensure(lambda s: len(s) &gt; 5, message='Too short')\n\n&gt;&gt;&gt; my_data.parse('hello')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Too short']}]\n</code></pre> <p>Use a callable object as the message.</p> <pre><code>&gt;&gt;&gt; my_data = (\n...   z.ensure(\n...       lambda s: len(s) &gt; 5,\n...       message=lambda s: f\"The {s!r} is too short\"\n...   )\n... )\n\n&gt;&gt;&gt; my_data.parse('hello')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': [\"The 'hello' is too short\"]}]\n</code></pre>"},{"location":"#break_on_failure","title":"break_on_failure","text":"<p>When multiple <code>ensure</code> methods are adjacent, even if one <code>ensure</code> fails, the subsequent <code>ensure</code> methods will still be executed by default.</p> <pre><code>&gt;&gt;&gt; try:\n...   (\n...     z.ensure(lambda s: len(s) &gt;= 6, message='Password must be at least 6 characters')\n...      .ensure(lambda s: \"!\" not in s, message='Password must not contain !')\n...   ).parse('ab12!')\n... except z.ValidationError as e:\n...   e.format_errors()\n...\n[{'msgs': ['Password must be at least 6 characters', 'Password must not contain !']}]\n</code></pre> <p>The <code>break_on_failure</code> parameter controls whether the validation should terminate upon failure, preventing further validation from being propagated.</p> <pre><code>&gt;&gt;&gt; try:\n...   (\n...     z.ensure(lambda s: len(s) &gt;= 6, break_on_failure=True, message='Password must be at least 6 characters')\n...      .ensure(lambda s: \"!\" not in s, message='Password must not contain !')\n...   ).parse('ab12!')\n... except z.ValidationError as e:\n...   e.format_errors()\n...\n[{'msgs': ['Password must be at least 6 characters']}]\n</code></pre>"},{"location":"#transform","title":"<code>.transform</code>","text":"<p>To transform data during parsing, use the <code>transform</code> method.</p> <pre><code>&gt;&gt;&gt; to_int = z.transform(lambda s: int(s))\n\n&gt;&gt;&gt; to_int.parse('10')\n10\n</code></pre>"},{"location":"#message_1","title":"message","text":"<p>A <code>message</code> can be provided for a transformation method, which will be used when transformation fails. If the <code>message</code> is a callable object, its return value will be used as the <code>message</code>.</p> <pre><code>&gt;&gt;&gt; z.transform(lambda x: int(x), message='Invalid integer').parse('a')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Invalid integer']}]\n</code></pre> <p>Use a callable object as the message.</p> <pre><code>&gt;&gt;&gt; z.transform(lambda x: int(x), message=lambda x: f'Invalid integer: {x!r}').parse('a')\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': [\"Invalid integer: 'a'\"]}]\n</code></pre>"},{"location":"#relay","title":"<code>.relay</code>","text":"<p>The <code>relay</code> method is simply a shortcut for invoking another schema for parsing using <code>transform</code>. It is equivalent to the following code:</p> <pre><code>&gt;&gt;&gt; my_int = z.transform(int).relay(z.int())\n\n# equivalent to\n&gt;&gt;&gt; my_int = z.transform(int).transform(z.int().parse)\n</code></pre>"},{"location":"#parse","title":"<code>.parse</code>","text":"<p>Given any schema, you can call its <code>.parse</code> method to check data is valid. If it is, a value is returned with full type information! Otherwise, an error is thrown.</p> <pre><code>&gt;&gt;&gt; string = z.str()\n\n&gt;&gt;&gt; string.parse(\"hello\")\n'hello'\n\n&gt;&gt;&gt; string.parse(123)\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Expected str, received int']}]\n</code></pre>"},{"location":"#unions","title":"Unions","text":"<p>Zangar provides a way to create a union schema using the <code>|</code> operator, where the data is parsed sequentially from left to right.</p> <pre><code>&gt;&gt;&gt; int_or_str = z.int() | z.str()\n\n&gt;&gt;&gt; int_or_str.parse(1)\n1\n\n&gt;&gt;&gt; int_or_str.parse('1')\n'1'\n\n&gt;&gt;&gt; int_or_str.parse(None)\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'msgs': ['Expected int, received NoneType', 'Expected str, received NoneType']}]\n</code></pre>"},{"location":"#typing","title":"Typing","text":"<p><code>z.Schema</code> is an interface, you can use <code>z.Schema[T]</code> to specify the type of the data. This can be helpful for static type checking.</p> <pre><code>from typing import TypeVar\n\nT = TypeVar('T')\n\ndef your_function(schema: z.Schema[T]) -&gt; T:\n    # do something\n    return schema.parse(...)\n</code></pre>"},{"location":"dataclass/","title":"Dataclass","text":""},{"location":"dataclass/#dataclass","title":"<code>dataclass</code>","text":"<p>The <code>dataclass</code> method converts a <code>dataclasses.dataclass</code> into a Zangar schema for data validation, and the resulting output is an instance of that dataclass.</p>"},{"location":"dataclass/#create-a-dataclass","title":"Create a dataclass","text":"<pre><code>&gt;&gt;&gt; from dataclasses import dataclass\n&gt;&gt;&gt; import zangar as z\n\n&gt;&gt;&gt; @dataclass\n... class InventoryItem:\n...     \"\"\"Class for keeping track of an item in inventory.\"\"\"\n...     name: str\n...     unit_price: float\n...     quantity_on_hand: int = 0\n</code></pre> <pre><code># It's good!\n&gt;&gt;&gt; z.dataclass(InventoryItem).parse({'name': \"necklace\", 'unit_price': 12.50})\nInventoryItem(name='necklace', unit_price=12.5, quantity_on_hand=0)\n</code></pre> <pre><code># It's bad! because unit_price is not a float.\n&gt;&gt;&gt; z.dataclass(InventoryItem).parse({'name': \"necklace\", 'unit_price': '12.50'})\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'loc': ['unit_price'], 'msgs': ['Expected float, received str']}]\n</code></pre>"},{"location":"dataclass/#customize-the-field-schema","title":"Customize the field schema","text":"<pre><code>&gt;&gt;&gt; from dataclasses import field\n\n&gt;&gt;&gt; @dataclass\n... class InventoryItem:\n...     \"\"\"Class for keeping track of an item in inventory.\"\"\"\n...     name: str\n...     unit_price: float = field(metadata={'zangar_schema': z.transform(float)})\n...     quantity_on_hand: int = 0\n\n&gt;&gt;&gt; z.dataclass(InventoryItem).parse({'name': \"necklace\", 'unit_price': '12.50'})\nInventoryItem(name='necklace', unit_price=12.5, quantity_on_hand=0)\n</code></pre>"},{"location":"dataclass/#nested-dataclasses","title":"Nested dataclasses","text":"<pre><code>&gt;&gt;&gt; from typing import List\n\n&gt;&gt;&gt; @dataclass\n... class Point:\n...      x: int\n...      y: int\n\n&gt;&gt;&gt; @dataclass\n... class C:\n...      mylist: List[Point]\n\n&gt;&gt;&gt; z.dataclass(C).parse({'mylist': [{'x': 1, 'y': 2}, {'x': 3, 'y': 4}]})\nC(mylist=[Point(x=1, y=2), Point(x=3, y=4)])\n\n&gt;&gt;&gt; z.dataclass(C).parse({'mylist': [{'x': 1, 'y': 2}, {'x': 3, 'y': '4'}]})\nTraceback (most recent call last):\nzangar.exceptions.ValidationError: [{'loc': ['mylist', 1, 'y'], 'msgs': ['Expected int, received str']}]\n</code></pre>"},{"location":"exceptions/","title":"Exceptions","text":""},{"location":"exceptions/#validationerror","title":"ValidationError","text":""},{"location":"exceptions/#format_errors","title":"<code>.format_errors</code>","text":"<pre><code>&gt;&gt;&gt; name_schema = (\n...     z.str()\n...     .ensure(\n...         lambda x: len(x) &gt;= 5,\n...         message=\"The name is at lease 5 characters long\",\n...     )\n...     .ensure(\n...         lambda s: \"!\" not in s,\n...         message=\"The name cannot contain !\",\n...     )\n... )\n\n&gt;&gt;&gt; try:\n...     z.object(\n...         {\n...             \"names\": z.field(\n...                 z.list(name_schema)\n...             ),\n...         }\n...     ).parse(\n...         {\n...             \"names\": [\"Isabella\", \"Olivia\", \"Ava!\"],\n...         }\n...     )\n... except z.ValidationError as e:\n...     e.format_errors()\n...\n[{'loc': ['names', 2], 'msgs': ['The name is at lease 5 characters long', 'The name cannot contain !']}]\n</code></pre> <p>Return result type</p> <pre><code>type Errors = Array&lt;{\n  loc?: Array&lt;string | number&gt;;\n  msgs: Array&lt;any&gt;;\n}&gt;;\n</code></pre>"},{"location":"methods/","title":"Methods","text":"<p>Zangar provides convenient validation methods for some types.</p> <p>For example:</p> <pre><code>&gt;&gt;&gt; z.str().strip().min(1).max(20).parse(' hello ')\n'hello'\n\n# equivalent to:\n&gt;&gt;&gt; z.str().transform(str.strip).ensure(lambda x: len(x) &gt;= 1).ensure(lambda x: len(x) &lt;= 20).parse(' hello ')\n'hello'\n</code></pre>"},{"location":"methods/#string","title":"String","text":""},{"location":"methods/#max","title":"<code>.max</code>","text":"<p>Validate the maximum length of a string.</p> <pre><code>&gt;&gt;&gt; string = z.str().max(20)\n\n# equivalent to:\n&gt;&gt;&gt; string = z.str().ensure(lambda x: len(x) &lt;= 20)\n</code></pre>"},{"location":"methods/#min","title":"<code>.min</code>","text":"<p>Validate the minimum length of a string.</p> <pre><code>&gt;&gt;&gt; string = z.str().min(1)\n\n# equivalent to:\n&gt;&gt;&gt; string = z.str().ensure(lambda x: len(x) &gt;= 1)\n</code></pre>"},{"location":"methods/#strip","title":"<code>.strip</code>","text":"<p>Trim whitespace from both ends.</p> <pre><code>&gt;&gt;&gt; z.str().strip().parse(' string ')\n'string'\n\n# equivalent to:\n&gt;&gt;&gt; z.str().transform(str.strip).parse(' string ')\n'string'\n</code></pre>"},{"location":"methods/#number","title":"Number","text":""},{"location":"methods/#gt","title":"<code>.gt</code>","text":"<p>Validate the number is greater than a given value.</p> <pre><code>&gt;&gt;&gt; number = z.int().gt(0)\n\n# equivalent to:\n&gt;&gt;&gt; number = z.int().ensure(lambda x: x &gt; 0)\n</code></pre>"},{"location":"methods/#gte","title":"<code>.gte</code>","text":"<p>Validate the number is greater than or equal to a given value.</p> <pre><code>&gt;&gt;&gt; number = z.int().gte(0)\n\n# equivalent to:\n&gt;&gt;&gt; number = z.int().ensure(lambda x: x &gt;= 0)\n</code></pre>"},{"location":"methods/#lt","title":"<code>.lt</code>","text":"<p>Validate the number is less than a given value.</p> <pre><code>&gt;&gt;&gt; number = z.int().lt(10)\n\n# equivalent to:\n&gt;&gt;&gt; number = z.int().ensure(lambda x: x &lt; 10)\n</code></pre>"},{"location":"methods/#lte","title":"<code>.lte</code>","text":"<p>Validate the number is less than or equal to a given value.</p> <pre><code>&gt;&gt;&gt; number = z.int().lte(10)\n\n# equivalent to:\n&gt;&gt;&gt; number = z.int().ensure(lambda x: x &lt;= 10)\n</code></pre>"},{"location":"types/","title":"Types","text":""},{"location":"types/#primitives","title":"Primitives","text":"<pre><code>&gt;&gt;&gt; my_str = z.str()             # Validate that the data is of type `str`.\n&gt;&gt;&gt; my_int = z.int()             # Validate that the data is of type `int`.\n&gt;&gt;&gt; my_float = z.float()         # Validate that the data is of type `float`.\n&gt;&gt;&gt; my_bool = z.bool()           # Validate that the data is of type `bool`.\n&gt;&gt;&gt; my_none = z.none()           # Validate that the data is of type `None`.\n&gt;&gt;&gt; my_datatime = z.datetime()   # Validate that the data is of type `datetime`.\n&gt;&gt;&gt; my_any = z.any()             # Validate that the data is of any type.\n</code></pre> <p>These schemas are simple type validations and do not provide type conversion. Here's an equivalent example:</p> <pre><code>&gt;&gt;&gt; string = z.str()\n\n# equivalent to:\n&gt;&gt;&gt; string = z.ensure(lambda x: isinstance(x, str))\n</code></pre>"},{"location":"types/#list","title":"List","text":"<pre><code>&gt;&gt;&gt; z.list(z.transform(int)).parse(['1', 2, '3'])\n[1, 2, 3]\n\n# equivalent to:\n&gt;&gt;&gt; z.ensure(lambda x: isinstance(x, list)).transform(lambda x: [z.transform(int).parse(i) for i in x]).parse(['1', 2, '3'])\n[1, 2, 3]\n</code></pre>"},{"location":"types/#object","title":"Object","text":"<p><code>object</code> is a schema with fields, it can parse any object and return a dict.</p> <pre><code>&gt;&gt;&gt; dog = z.object({\n...     'name': z.field(z.str()),\n...     'breed': z.field(z.str()),\n... })\n</code></pre> <p>The <code>object</code> field, when parsing an object, will use its field name to get the value of the key for <code>Mapping</code> objects, and the value of the attribute for other objects.</p> <pre><code># Parsing a Mapping object\n\n&gt;&gt;&gt; dog.parse({\n...     'name': 'Fido',\n...     'breed': 'bulldog'\n... })\n{'name': 'Fido', 'breed': 'bulldog'}\n</code></pre> <pre><code># Parsing other objects\n\n&gt;&gt;&gt; from types import SimpleNamespace\n\n&gt;&gt;&gt; obj = SimpleNamespace(name='Fido', breed='bulldog')\n&gt;&gt;&gt; dog.parse(obj)\n{'name': 'Fido', 'breed': 'bulldog'}\n</code></pre>"},{"location":"types/#field","title":"Field","text":""},{"location":"types/#alias","title":"<code>alias</code>","text":"<p>You can assign alias to external data corresponding to field, which will be mapped to field name during parsing.</p> <pre><code>&gt;&gt;&gt; dog = z.object({\n...   'name': z.field(z.str(), alias='nickname'),\n... })\n\n&gt;&gt;&gt; dog.parse({'nickname': 'Fido'})\n{'name': 'Fido'}\n</code></pre>"},{"location":"types/#optional","title":"<code>.optional</code>","text":"<p>Fields are required by default, but this method allows them to be made optional.</p> <pre><code>&gt;&gt;&gt; dog = z.object({\n...     'name': z.field(z.str()),\n...     'breed': z.field(z.str()).optional(),\n... })\n\n&gt;&gt;&gt; dog.parse({'name': 'Fido'})\n{'name': 'Fido'}\n</code></pre> <p>It is also possible to provide a default value for the optional field.</p> <pre><code>&gt;&gt;&gt; dog = z.object({\n...     'name': z.field(z.str()),\n...     'breed': z.field(z.str()).optional(default='unknown'),\n... })\n\n&gt;&gt;&gt; dog.parse({'name': 'Fido'})\n{'name': 'Fido', 'breed': 'unknown'}\n</code></pre>"},{"location":"types/#extend","title":"<code>.extend</code>","text":"<p>You can add additional fields to an object schema with the .extend method.</p> <pre><code>&gt;&gt;&gt; dog_with_age = dog.extend({\n...   'age': z.field(z.int()),\n... })\n</code></pre>"}]}